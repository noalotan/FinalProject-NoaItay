pipeline {
    agent any
    stages {
        stage('Architecture Apply') {
            steps {
                script {
                    status 'Architecture Apply', 'PENDING'
                    try {
                        build job: 'Dev-Architecture-apply'
                        status 'Architecture Apply', 'SUCCESS'
                    } catch (Exception e) {
                        status 'Architecture Apply', 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Image Build') {
            steps {
                script {
                    status 'Image Build', 'PENDING'
                    try {
                        build job: 'Dev-Update-status-page-image'
                        status 'Image Build', 'SUCCESS'
                    } catch (Exception e) {
                        status 'Image Build', 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Image Test') {
            steps {
                script {
                    status 'Image Test', 'PENDING'
                    try {
                        build job: 'Dev-app-syntax-test'
                        status 'Image Test', 'SUCCESS'
                    } catch (Exception e) {
                        status 'Image Test', 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Deploy YMLs') {
            steps {
                script {
                    status 'Deploy YMLs', 'PENDING'
                    try {
                        build job: 'Dev-Application-deploy'
                        status 'Deploy YMLs', 'SUCCESS'
                    } catch (Exception e) {
                        status 'Deploy YMLs', 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('ALB Apply') {
            steps {
                script {
                    status 'ALB Apply', 'PENDING'
                    try {
                        build job: 'Dev-Alb-deploy'
                        status 'ALB Apply', 'SUCCESS'
                    } catch (Exception e) {
                        status 'ALB Apply', 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Tests') {
            steps {
                script {
                    status 'Tests', 'PENDING'
                    try {
                        build job: 'Dev-CI-Tests'
                        status 'Tests', 'SUCCESS'
                    } catch (Exception e) {
                        status 'Tests', 'FAILURE'
                        throw e
                    }
                }
            }
        }
    }
}
