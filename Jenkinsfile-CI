pipeline {
    agent any
    stages {
        stage('Architecture Apply') {
            steps {
                script {
                    githubNotify context: 'Architecture Apply', status: 'PENDING'
                    try {
                        build job: 'Dev-Architecture-apply'
                        githubNotify context: 'Architecture Apply', status: 'SUCCESS'
                    } catch (Exception e) {
                        githubNotify context: 'Architecture Apply', status: 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Image Build') {
            steps {
                script {
                    githubNotify context: 'Image Build', status: 'PENDING'
                    try {
                        build job: 'Dev-Update-status-page-image'
                        githubNotify context: 'Image Build', status: 'SUCCESS'
                    } catch (Exception e) {
                        githubNotify context: 'Image Build', status: 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Image Test') {
            steps {
                script {
                    githubNotify context: 'Image Test', status: 'PENDING'
                    try {
                        build job: 'Dev-app-syntax-test'
                        githubNotify context: 'Image Test', status: 'SUCCESS'
                    } catch (Exception e) {
                        githubNotify context: 'Image Test', status: 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Deploy YMLs') {
            steps {
                script {
                    githubNotify context: 'Deploy YMLs', status: 'PENDING'
                    try {
                        build job: 'Dev-Application-deploy'
                        githubNotify context: 'Deploy YMLs', status: 'SUCCESS'
                    } catch (Exception e) {
                        githubNotify context: 'Deploy YMLs', status: 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('ALB Apply') {
            steps {
                script {
                    githubNotify context: 'ALB Apply', status: 'PENDING'
                    try {
                        build job: 'Dev-Alb-deploy'
                        githubNotify context: 'ALB Apply', status: 'SUCCESS'
                    } catch (Exception e) {
                        githubNotify context: 'ALB Apply', status: 'FAILURE'
                        throw e
                    }
                }
            }
        }
        stage('Tests') {
            steps {
                script {
                    githubNotify context: 'Tests', status: 'PENDING'
                    try {
                        build job: 'Dev-CI-Tests'
                        githubNotify context: 'Tests', status: 'SUCCESS'
                    } catch (Exception e) {
                        githubNotify context: 'Tests', status: 'FAILURE'
                        throw e
                    }
                }
            }
        }
    }
}
